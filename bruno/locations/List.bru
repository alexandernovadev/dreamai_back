meta {
  name: List Locations
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/locations
  body: none
  auth: none
}

docs {
  Optional query params: page, limit, name, nameExact, description, isFamiliar, setting,
  hasImage, createdFrom, createdTo, updatedFrom, updatedTo
}
