# MongoDB local (Mongoose; no replica set obligatorio para CRUD).

services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    ports:
      - '27017:27017'
    volumes:
      - dreamia_mongo_data:/data/db

volumes:
  dreamia_mongo_data:
