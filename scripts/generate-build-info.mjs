/**
 * Invoked from `yarn build` / `npm run build` before `nest build`.
 * Writes ISO timestamp of the build into `src/build-info.generated.ts`.
 */
import { writeFileSync } from 'node:fs';
import { dirname, join } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const root = join(__dirname, '..');
const outFile = join(root, 'src', 'build-info.generated.ts');
const iso = new Date().toISOString();

writeFileSync(
  outFile,
  `// Auto-generated by scripts/generate-build-info.mjs — do not edit.\nexport const BUILD_ISO_DATE = '${iso}';\n`,
  'utf8',
);
